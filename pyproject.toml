[project]
name = "handmade-gpuer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Benny Zhu", email = "developer.benny.zhu@proton.me" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
handmade-gpuer = "handmade_gpuer:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.57",
]
